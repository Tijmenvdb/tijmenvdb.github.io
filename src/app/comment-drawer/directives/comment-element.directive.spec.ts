import { CommentElementDirective } from './comment-element.directive';

describe('CommentElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentElementDirective();
    expect(directive).toBeTruthy();
  });
});
