import { SectionOrderPipe } from './section-order.pipe';

describe('CommentSectionOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
